services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: polymarket
      POSTGRES_PASSWORD: polymarket
      POSTGRES_DB: polymarket_history
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
